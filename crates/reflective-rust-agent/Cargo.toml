[package]
name = "reflective-rust-agent"
version = "0.1.0"
edition = "2024"
authors = ["Reflective Rust Research Team"]
description = "Compiler-certified Semantic Graph Slicing engine feeding LLM agents exact type and privacy contexts."
license = "MIT OR Apache-2.0"

[dependencies]
reflective-rust-csg = { path = "../reflective-rust-csg" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
